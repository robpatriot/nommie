# WebSocket Architecture

- Mutations must occur via HTTP endpoints.
- WebSocket is server-to-client broadcast only.
- Outbound messages must include version.
- Broadcasts are triggered by HTTP mutations.
