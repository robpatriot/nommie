# Frontend Rules

Applies to apps/frontend.

- Keep changes goal-scoped.
- Follow existing Next.js App Router patterns.
- Keep types consistent with shared packages in packages/.
- If user-visible behavior changes, ensure appropriate tests or docs are updated.
