# Public backend URL (required). Used by browser for WebSocket and by next.config.
# Stored here so it's available for build and runtime.
NEXT_PUBLIC_BACKEND_BASE_URL=https://api.example.com

# Optional: internal URL for server-side fe->be calls. When set, server uses this
# instead of NEXT_PUBLIC_BACKEND_BASE_URL for auth, JWT refresh, and API requests.
# BACKEND_BASE_URL=http://backend:3001
