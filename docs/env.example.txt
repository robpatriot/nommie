# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# Application Database Password (for nommie_app role)
APP_DB_PASSWORD=nommie_app_password

# Database URLs
# Main database connection (uses nommie_app role)
DATABASE_URL=postgresql://nommie_app:${APP_DB_PASSWORD}@localhost:${POSTGRES_PORT}/nommie

# Test database connection (uses nommie_app role)
TEST_DATABASE_URL=postgresql://nommie_app:${APP_DB_PASSWORD}@localhost:${POSTGRES_PORT}/nommie_test

# Copy this file to .env and adjust values as needed
# cp docs/env.example.txt .env

