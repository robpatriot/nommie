# Cookie file template for performance profiling authenticated pages
#
# INSTRUCTIONS:
# 1. Log in to your app in a browser at http://localhost:3000
# 2. Open DevTools (F12) → Application tab → Cookies → localhost:3000
# 3. Copy the values for these two cookies:
#    - authjs.session-token (NextAuth session token)
#    - backend_jwt (Backend JWT token)
# 4. Replace the placeholders below with your actual cookie values
# 5. Save this file as cookies.txt in the scripts/ directory
# 6. Use with: bash scripts/profile-performance.sh --cookie-file scripts/cookies.txt --url http://localhost:3000/game/123
#
# NOTE: This file contains sensitive authentication tokens. Never commit it to git.
#       It is already in .gitignore for your safety.

# NextAuth session token (required for frontend authentication)
authjs.session-token=PASTE_YOUR_SESSION_TOKEN_HERE

# Backend JWT token (required for API authentication)
backend_jwt=PASTE_YOUR_BACKEND_JWT_HERE

